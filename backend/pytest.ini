[pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    docker: needs a working Docker daemon (skipped automatically without one)
    browser: drives the dashboard in Chromium (skipped without `playwright install chromium`)
asyncio_mode = auto
