"""The `tracetensor` command-line interface (Typer + Rich).

A first-class door into the platform, next to the web dashboard — same backend,
so a run started here shows up in the UI. See app/cli/main.py for the command tree.
"""
