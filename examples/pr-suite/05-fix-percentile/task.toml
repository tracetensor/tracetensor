schema_version = "1.3"

[task]
name = "tracetensor/pr-suite/05-fix-percentile"
description = "PR-style: fix linear-interpolation percentile."
keywords = ["python", "bugfix", "pr", "hard"]

[metadata]
difficulty_explanation = "hard — off-by-one in percentile index math."
category = "programming"

[verifier]
timeout_sec = 60.0

[agent]
timeout_sec = 400.0

[environment]
network_mode = "public"
build_timeout_sec = 120.0
