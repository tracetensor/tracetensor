schema_version = "1.3"

[task]
name = "tracetensor/pr-suite/02-fix-deep-merge"
description = "PR-style: fix deep_merge for nested dicts."
keywords = ["python", "bugfix", "pr", "hard"]

[metadata]
difficulty_explanation = "hard — recursive merge overwrites nested dicts."
category = "programming"

[verifier]
timeout_sec = 60.0

[agent]
timeout_sec = 400.0

[environment]
network_mode = "public"
build_timeout_sec = 120.0
