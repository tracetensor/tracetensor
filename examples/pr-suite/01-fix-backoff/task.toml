schema_version = "1.3"

[task]
name = "tracetensor/pr-suite/01-fix-backoff"
description = "PR-style: fix exponential retry backoff (was linear)."
keywords = ["python", "bugfix", "pr", "hard"]

[metadata]
difficulty_explanation = "hard — subtle math bug in retry helper."
category = "programming"

[verifier]
timeout_sec = 60.0

[agent]
timeout_sec = 400.0

[environment]
network_mode = "public"
build_timeout_sec = 120.0
