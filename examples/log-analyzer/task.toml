schema_version = "1.3"

[task]
name = "tracetensor/log-analyzer"
description = "Parse an Nginx access log and produce a JSON statistics report."
authors = [{ name = "Rahul", email = "rahul@lh2.com" }]
keywords = ["logs", "parsing", "json", "data-analysis"]

[metadata]
difficulty_explanation = "Requires log parsing, aggregation, and precise JSON output."
category = "data-processing"

[verifier]
timeout_sec = 90.0

[agent]
timeout_sec = 180.0

[environment]
network_mode = "no-network"
build_timeout_sec = 600.0
