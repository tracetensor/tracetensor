schema_version = "1.3"

[task]
name        = "examples/multi-step-demo"
description = "Two-step demo: create a file, then append a line. Shared sandbox across steps."
keywords    = ["multi-step", "demo"]

[environment]
network_mode = "no-network"
