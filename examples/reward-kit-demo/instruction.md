Create a file at `/app/output.txt`.

Requirements:
1. The file must contain the word "correct" somewhere in it (any case).
2. The total file content must be under 50 characters (including whitespace).

Write only the file — no extra output.
