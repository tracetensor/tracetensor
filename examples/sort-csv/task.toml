schema_version = "1.3"

[task]
name = "tracetensor/sort-csv"
description = "Sort a CSV file by the revenue column, descending."
authors = [{ name = "Rahul" }]
keywords = ["csv", "sorting", "programming"]

[metadata]
difficulty_explanation = "Basic file manipulation task."
category = "programming"

[verifier]
timeout_sec = 60.0

[agent]
timeout_sec = 120.0

[environment]
network_mode = "no-network"
build_timeout_sec = 300.0
