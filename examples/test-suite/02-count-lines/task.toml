schema_version = "1.3"

[task]
name = "tracetensor/test-suite/02-count-lines"
description = "Count lines in a text file and write the count."
keywords = ["file-io", "easy"]

[metadata]
difficulty_explanation = "easy — read a file and write a numeric result."
category = "programming"

[verifier]
timeout_sec = 30.0

[agent]
timeout_sec = 60.0

[environment]
network_mode = "no-network"
build_timeout_sec = 120.0
