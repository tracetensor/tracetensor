A text file is at `/data/notes.txt`.

Count how many lines it contains and write **only the number** (no extra text) to
`/data/line_count.txt`.

Example: if the file has 5 lines, `/data/line_count.txt` should contain exactly
`5`.
