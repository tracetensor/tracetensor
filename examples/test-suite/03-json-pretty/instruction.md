A minified JSON file is at `/data/minified.json`.

Read it and write a pretty-printed version to `/data/pretty.json` with:

- 2-space indentation
- keys sorted alphabetically at every object level

The parsed JSON content must match the input (same values and structure).
