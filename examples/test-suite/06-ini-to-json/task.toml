schema_version = "1.3"

[task]
name = "tracetensor/test-suite/06-ini-to-json"
description = "Parse INI config into typed JSON."
keywords = ["ini", "json", "medium"]

[metadata]
difficulty_explanation = "medium — structured parsing with type coercion."
category = "data-processing"

[verifier]
timeout_sec = 45.0

[agent]
timeout_sec = 120.0

[environment]
network_mode = "no-network"
build_timeout_sec = 120.0
