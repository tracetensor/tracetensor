schema_version = "1.3"

[task]
name = "tracetensor/test-suite/04-fix-multiply"
description = "Fix a one-line bug in multiply(a, b)."
keywords = ["python", "bugfix", "medium"]

[metadata]
difficulty_explanation = "medium — small bugfix in an existing module."
category = "programming"

[verifier]
timeout_sec = 30.0

[agent]
timeout_sec = 300.0

[environment]
network_mode = "public"
build_timeout_sec = 120.0
