schema_version = "1.3"

[task]
name = "tracetensor/test-suite/07-word-frequency"
description = "Compute top-5 word frequencies from prose."
keywords = ["text", "json", "medium"]

[metadata]
difficulty_explanation = "medium-hard — tokenization, counting, tie-breaking."
category = "data-processing"

[verifier]
timeout_sec = 60.0

[agent]
timeout_sec = 180.0

[environment]
network_mode = "no-network"
build_timeout_sec = 120.0
