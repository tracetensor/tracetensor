schema_version = "1.3"

[task]
name = "tracetensor/test-suite/10-sales-report"
description = "Filter CSV sales and aggregate by region."
keywords = ["csv", "aggregation", "hard"]

[metadata]
difficulty_explanation = "hard — filter, group-by, strict JSON report."
category = "data-processing"

[verifier]
timeout_sec = 60.0

[agent]
timeout_sec = 240.0

[environment]
network_mode = "no-network"
build_timeout_sec = 120.0
