A CSV file is at `/data/users.csv` with columns `email,name`.

Remove duplicate rows by `email`, keeping the **first** occurrence of each email.
Write the result to `/data/users_unique.csv` with the same header and column order.
