schema_version = "1.3"

[task]
name = "tracetensor/test-suite/08-merge-records"
description = "Join CSV orders with JSON customers."
keywords = ["csv", "json", "join", "hard"]

[metadata]
difficulty_explanation = "hard — multi-file join with strict output schema."
category = "data-processing"

[verifier]
timeout_sec = 60.0

[agent]
timeout_sec = 240.0

[environment]
network_mode = "no-network"
build_timeout_sec = 120.0
