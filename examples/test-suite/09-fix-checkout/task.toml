schema_version = "1.3"

[task]
name = "tracetensor/test-suite/09-fix-checkout"
description = "Fix tax rate and discount order in a small checkout module."
keywords = ["python", "bugfix", "hard"]

[metadata]
difficulty_explanation = "hard — multi-file business logic bugfix."
category = "programming"

[verifier]
timeout_sec = 60.0

[agent]
timeout_sec = 400.0

[environment]
network_mode = "public"
build_timeout_sec = 120.0
