schema_version = "1.3"

[task]
name = "tracetensor/agentic-suite/03-fix-session-auth"
description = "Agentic: fix session lookup across JSON stores."
keywords = ["python", "bugfix", "agentic", "hard"]

[metadata]
difficulty_explanation = "hard — two JSON files + session validation."
category = "programming"

[verifier]
timeout_sec = 90.0

[agent]
timeout_sec = 500.0

[environment]
network_mode = "public"
build_timeout_sec = 120.0
