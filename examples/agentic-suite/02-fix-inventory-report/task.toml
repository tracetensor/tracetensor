schema_version = "1.3"

[task]
name = "tracetensor/agentic-suite/02-fix-inventory-report"
description = "Agentic: fix CSV inventory rollup with config."
keywords = ["python", "bugfix", "agentic", "medium"]

[metadata]
difficulty_explanation = "medium — CSV + config.json interaction."
category = "programming"

[verifier]
timeout_sec = 90.0

[agent]
timeout_sec = 500.0

[environment]
network_mode = "public"
build_timeout_sec = 120.0
