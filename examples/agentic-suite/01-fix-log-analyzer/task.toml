schema_version = "1.3"

[task]
name = "tracetensor/agentic-suite/01-fix-log-analyzer"
description = "Agentic: fix multi-step log level counter across files."
keywords = ["python", "bugfix", "agentic", "medium"]

[metadata]
difficulty_explanation = "medium — read logs + fix parser module."
category = "programming"

[verifier]
timeout_sec = 90.0

[agent]
timeout_sec = 500.0

[environment]
network_mode = "public"
build_timeout_sec = 120.0
